DashboardPage.razor (Home.razor)                (Blazor, Pages)
        │  OnInitializedAsync
        ▼
IDashboardClientService                         (Shared/Services/Dashboards)
        │  implement bởi
        ▼
DashboardClientService                          (Shared/Services/Dashboards)
        │  HTTP GET api/dashboard
        ▼
DashboardController                             (API)
        │  gọi
        ▼
IDashboardOrchestrator                          (Application/Interfaces/Orchestrators)
        │  implement bởi
        ▼
DashboardOrchestrator                           (Application/Orchestrators)
        │  1. gọi IApprovalInboxService để lấy Pending ĐA MODULE (đã có sẵn, không tự query lại)
        │  2. gọi từng IModuleDashboardProvider (1 provider / module) để lấy Widgets + Detail
        │  3. gọi DashboardWidgetPolicy để lọc/sắp xếp Widgets theo quyền user
        ▼
IModuleDashboardProvider                        (Application/Interfaces/Dashboards — contract, mỗi module tự biết cách lấy dữ liệu của mình)
        │  implement bởi (đăng ký DI dạng collection, thêm module mới KHÔNG sửa Orchestrator)
        ├─ LeaveDashboardProvider    (Infrastructure/Services/Dashboards) → dùng ILeaveQueryService + IStatisticsService
        ├─ OTDashboardProvider       (Infrastructure/Services/Dashboards) → dùng IOTQueryService + ...
        └─ TripDashboardProvider     (thêm sau khi module Trip có Query service — CHƯA build)
        ▼
DashboardWidgetPolicy                           (Application/Policies — THUẦN LOGIC, không I/O, đúng vai trò như NotificationFactory)
        ▼
DashboardResponse (Contract/Responses)           — DTO tổng hợp trả về client
        ▼
DashboardClientService hiển thị → DashboardOverview.razor / UserDashboard.razor